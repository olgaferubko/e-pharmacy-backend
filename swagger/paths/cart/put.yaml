tags:
  - Cart
summary: Update Cart
description: "Update the quantity of items in the user's cart"
operationId: updateCart
security:
  - bearerAuth: []
requestBody:
  content:
    application/json:
      schema:
        $ref: '../../components/schemas/updateCartRequest.yaml'
responses:
  '200':
    description: 'Updated cart'
    content:
      application/json:
        schema:
          $ref: '../../components/schemas/cart.yaml'
  '401':
    $ref: '../../components/responses/401.yaml'
